
.global fib_iter_s

fib_iter_s:
	MOV r1, #0
	MOV r2, #1
	MOV r3, #1

	CMP r0, #0
	bne loop
	MOV r0, r3
	bx lr
	
	loop:
	MOV r1, r2		MOV r2, r3
	ADD r3, r1, r2
	SUB r0, r0, #1
	CMP r0, #0
	bne loop
	
	
